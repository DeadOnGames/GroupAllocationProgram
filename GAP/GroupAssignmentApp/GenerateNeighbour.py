import numpy as np


def generate_neighbour(td):

    l = len(td)
    if l == 0:
        return [[]]

    for i in range(l):
        tdl = td[i]
        m = len(tdl)
        tdi = td[i]
        for j in range(m):
            if j == 0:

        

generate_neighbour([[1,2,3]])
